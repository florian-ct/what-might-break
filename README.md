# what-might-break

Analyze your code base to understand what will break before you touch anything.

- MVP:
- - User can run the tool on their code base via the CLI
- - User can specify a file or directory to analyze
- - Tool outputs a list of the most dependent on files in the code base
